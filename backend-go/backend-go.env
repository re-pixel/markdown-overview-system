# Deprecated: use root .env file instead. Remove this file from production images.
# Provide real credentials via environment at runtime.
DATABASE_URL=postgresql://username:password@host:5432/dbname?sslmode=require
PORT=8080